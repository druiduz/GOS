<Page x:Class="GOS.Pages.Clients"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Clients">

    <Grid>
        <DataGrid ItemsSource="{Binding Clients}" AutoGenerateColumns="False" Height="251" HorizontalAlignment="Left" Margin="0,49,0,0" Name="ListeClients" VerticalAlignment="Top" Width="291" SelectionChanged="ListeClients_SelectionChanged" ItemsSource="{Binding ElementName=ListeClients, Path=CellsPanelHorizontalOffset}" />
        <Label Content="Liste des clients" Height="28" HorizontalAlignment="Left" Margin="4,15,0,0" Name="Liste" VerticalAlignment="Top" />
    </Grid>
</Page>
